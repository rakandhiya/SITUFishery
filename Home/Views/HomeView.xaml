<UserControl x:Class="SITUFishery.Home.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SITUFishery.Home.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0">
            <TextBlock Text="Selamat Datang!" HorizontalAlignment="Center"  VerticalAlignment="Top" FontSize="26" FontWeight="Bold" />
            <Button x:Name="LoadPetakPage" Content="Data Petak" Width="100" Height="20" VerticalAlignment="Top" />
        </StackPanel>

        <ContentControl Grid.Row="1" x:Name="ChildScreen" Height="auto" />
    </Grid>
</UserControl>
