<UserControl x:Class="SITUFishery.Modules.PakanHarianModule.Views.NewPakanHarianView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SITUFishery.Modules.PakanHarianModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0" Grid.Column="0" x:Name="Petaks" 
                  SelectedItem="{Binding Path=SelectedPetak, Mode=OneWayToSource}"
                  DisplayMemberPath="NoPetak"/>
        <ComboBox Grid.Row="1" Grid.Column="0" x:Name="Pakans" 
                  SelectedItem="{Binding Path=SelectedPakan, Mode=OneWayToSource}"
                  DisplayMemberPath="Nama"/>
        <TextBox x:Name="Quantity" Grid.Row="2" Grid.Column="0" Width="100" />
        <DatePicker x:Name="Tanggal" Grid.Row="3" Grid.Column="0" />
        <Button x:Name="Submit" Grid.Row="4" Grid.Column="0"  Content="Submit" />
    </Grid>
</UserControl>
